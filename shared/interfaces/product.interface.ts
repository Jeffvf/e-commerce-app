export interface Product {
  id: string,
  nome: string,
  categoria: string,
  preco: number,
  desconto: number,
  quantidade: number,
  quantidadeAvaliacoes: number,
  vendas: number,
  nota: number
}